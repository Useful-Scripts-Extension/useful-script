window.onload = () => {
  // var addLink = () => true;

  // let attr = ["oncopy", "oncut", "onpaste", "onselectstart"];
  // Array.from(
  //   document.querySelectorAll(attr.map((key) => `[${key}]`).join(", "))
  // ).forEach((el) => {
  //   attr.forEach((key) => {
  //     el.removeAttribute(key);
  //   });
  // });
};
